= form_for @ignorlist do |f|
  - if @ignorlist.errors.any?
    #error_explanation
      %h2= "#{pluralize(@ignorlist.errors.count, "error")} prohibited this ignorlist from being saved:"
      %ul
        - @ignorlist.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :schedule
    = f.text_field :schedule
  .field
    = f.label :station
    = f.text_field :station
  .actions
    = f.submit 'Save'
